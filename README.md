# Bombus
Self-hostable chat application named after the bumblebee.
Run setup.sh & start.sh to start the server, create the first account with the username "admin" and no passcode/invite code to get started, the admin account can then invite others. Login is solely done using TOTP codes, you can use any authenticator app you'd like.
