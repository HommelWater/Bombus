# Bombus
Self-hostable chat application named after the bumblebee.

Setup:
1. Create vapid keys, just run the bash script.
2. Run the setup script, follow the instructions. Make sure you have a domain pointed at the server's IP adress.
3. Login for the first time with just "admin" as the username and an empty totp/invite code field.
