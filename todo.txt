Safety stuff, anti-spam/ddos stuff, max username lengths, clear up the input from HTML/CSS/JS (though make this optional, could be fun!).
Make this into a progressive web app.
Allow the user to find any message by id (like with search, add a button to find it in it's context) or by a link.
File upload, have images just be stored in interface/images and have actual files be separate. 
Probably allow users to upload then link to files with a tag in their chat or something. 
Maybe auto send a message with such a tag when a file gets uploaded. 
Add specific file viewer + copy file links, public or private settings etc.
Automatic NGINX setup for hosting on an actual server (user examia code as reference) etc.